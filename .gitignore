# MySQL binary / source (placeholder dirs kept via .gitkeep)
mysql960/*
!mysql960/.gitkeep
mysql960.source/*
!mysql960.source/.gitkeep
mysql970/
mysql970.source/
mysql-*.tar.xz
mysql-*.tar.gz

# Dev-only files (kept in local claude/ repo)
CLAUDE.md
claude/
plugins/my_calc_plugin/
local.mk

# Build artifacts
*.o
# `make` output for the native dev build (the released spatial_plugin-glibc-*.so binaries ARE tracked)
/plugins/spatial_plugin/spatial_plugin.so

# Claude Code config
.claude/

# Editor / IDE
.vscode/
*.swp
*~

# Gemini artifacts
*.gemini
