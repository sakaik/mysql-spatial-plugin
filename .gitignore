# MySQL binary / source (placeholder dirs kept via .gitkeep)
mysql960/*
!mysql960/.gitkeep
mysql960.source/*
!mysql960.source/.gitkeep
mysql-*.tar.xz
mysql-*.tar.gz

# Dev-only files (kept in local claude/ repo)
CLAUDE.md
claude/
plugins/my_calc_plugin/
local.mk

# Build artifacts
*.o
# spatial_plugin-glibc-*.so is tracked (not ignored)

# Claude Code config
.claude/

# Editor / IDE
.vscode/
*.swp
*~

# Gemini artifacts
*.gemini
