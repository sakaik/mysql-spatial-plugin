# MySQL binary / source
# - mysql970*: placeholder dirs kept in the repo via .gitkeep (contents ignored)
# - mysql960*: retired 9.6 env, kept locally for now, fully ignored
mysql970/*
!mysql970/.gitkeep
mysql970.source/*
!mysql970.source/.gitkeep
mysql960/
mysql960.source/
mysql-*.tar.xz
mysql-*.tar.gz

# Dev-only files (kept in local claude/ repo)
CLAUDE.md
claude/
plugins/my_calc_plugin/
local.mk

# Build artifacts
*.o
# `make` output for the native dev build (the released spatial_plugin-glibc-*.so binaries ARE tracked)
/plugins/spatial_plugin/spatial_plugin.so

# Claude Code config
.claude/

# Editor / IDE
.vscode/
*.swp
*~

# Gemini artifacts
*.gemini
